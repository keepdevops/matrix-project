{
  "name": "swarm-matrix-docker-vllm",
  "description": "Swarm Matrix configuration using Docker Model Runner + vLLM backend (single shared endpoint on port 12434)",
  "inference": {
    "backend": "docker-model-runner",
    "base_url": "http://127.0.0.1:12434/v1",
    "api_key": "EMPTY",
    "default_model": "Qwen2.5-7B-Instruct-4bit",
    "health_check": {
      "timeout_seconds": 600,
      "interval_seconds": 10,
      "retries": 60,
      "endpoints": ["/v1/models", "/health"]
    },
    "options": {
      "gpu_memory_utilization": 0.72,
      "max_model_len": 8192,
      "max_num_seqs": 48,
      "enforce_eager": true
    }
  },
  "agents": [
    {
      "name": "foreman",
      "role": "coordinator",
      "system_prompt": "You are the Engineering Foreman. Your sole job is structured planning and coordination — never write code. Given the current task: (1) List the 3–5 highest-priority implementation steps in order. (2) Assign each step to the most appropriate engineering role. (3) Flag any blockers. Output a numbered action plan only. Be concise and decisive.",
      "temperature": 0.3,
      "max_tokens": 1024
    },
    {
      "name": "programmer",
      "role": "coder",
      "system_prompt": "You are the Programmer. Produce complete, production-ready code with all imports, error handling, and full implementation. Never use placeholders.",
      "temperature": 0.7,
      "max_tokens": 4096
    },
    {
      "name": "debugger",
      "role": "analyzer",
      "system_prompt": "You are the Debug Specialist. Perform root cause analysis on bugs and failures. Propose diagnostic steps and targeted fixes.",
      "temperature": 0.4,
      "max_tokens": 2048
    },
    {
      "name": "tester",
      "role": "validator",
      "system_prompt": "You are the Tester. Create comprehensive test cases, edge cases, and validation strategies for the implemented code.",
      "temperature": 0.5,
      "max_tokens": 2048
    }
  ]
}
