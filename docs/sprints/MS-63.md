# MS-63 Sprint — Modular Split

**Branch:** `cursor/ms-63-sprint`
**Ceiling:** 110 LOC per file

## Files Over Ceiling

| File | LOC |
|---|---|
| `cpp_core/src/swarm_config_store.cpp` | 119 |
| `cpp_core/src/modes/cascade_exec.h` | 119 |
| `src/hooks/useSwarmSubmit.js` | 118 |
| `src/components/PresetsPanel.js` | 118 |
| `cpp_core/src/agent_client_pool.cpp` | 118 |
| `src/layouts/BrewMonitorPopout.js` | 117 |

## Planned Splits

### 1. `swarm_config_store.cpp` 119 → ~75 LOC
Extract `swarm_config_store_write.h` as `store_write` namespace inline (~45 LOC):
- `write_json_atomic(path, doc)` — atomic temp-file write helper
- `dual_write(paths, doc, outcome)` — writes to both active and source config paths
- `swarm_config_store.cpp` keeps `persist_agent_system_prompt`, `persist_agent_tokens`, public API

### 2. `cascade_exec.h` 119 → ~75 LOC
Extract `cascade_exec_synth.h` as `cascade_synth` namespace inline (~45 LOC):
- `run_synthesis(agents, cfg, synth_name, synth_agent, outputs, prompt)` — synthesis agent call + result assembly
- `build_synthesis_input(outputs, participants, prompt)` — constructs the synthesizer context string
- `cascade_exec.h` keeps parallel broadcast, result collection, top-level `run` dispatch

### 3. `useSwarmSubmit.js` 118 → ~75 LOC
Extract `useSwarmSubmit.stream.js` (~45 LOC):
- SSE callback builder: `buildStreamCallbacks(setters, cancelRef, opts)` — returns `{ onToken, onAgentDone, onSelected, onStage, onSynthesisStart, onMetrics, onDone, onError, onSession }`
- `useSwarmSubmit.js` keeps `submit` useCallback, backend switch, cancel logic

### 4. `PresetsPanel.js` 118 → ~75 LOC
Extract `usePresetsPanel.js` (~45 LOC):
- State: `presets`, `loading`, `error`, `saving`, `applying`, `activeMode`
- `handleSave`, `handleDelete`, `handleApply` async handlers
- `PresetsPanel.js` keeps render: preset list, save form, status messages

### 5. `agent_client_pool.cpp` 118 → ~75 LOC
Extract `agent_client_pool_queue.h` as `pool_queue` namespace inline (~45 LOC):
- `Pool` struct with `mu`, `cv`, `idle`, `busy` members
- `acquire(pool, host, port, timeout_ms)` — get or create a client from the pool
- `release(pool, client)` — return a client to the idle queue
- `agent_client_pool.cpp` keeps `agent_http_post` public function

### 6. `BrewMonitorPopout.js` 117 → ~75 LOC
Extract `BrewMonitorStats.js` (~45 LOC):
- KV aggregate display section (kvAggregate + stats grid)
- Per-agent status rows
- `BrewMonitorPopout.js` keeps popout chrome, header, PressureCluster, scroll ref

## Test Plan
- `npm test -- --watchAll=false` → 924 tests pass
- `pytest tests/test_modes.py tests/test_streaming.py -q` → 28 tests pass
