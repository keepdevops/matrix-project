# MS-55 Sprint — Modular Split

**Branch:** `cursor/ms-55-sprint`
**Ceiling:** 110 LOC per file

## Files Over Ceiling

| File | LOC |
|---|---|
| `src/components/CodeDisplay.js` | 153 |
| `src/components/AppHeader.js` | 150 |
| `src/components/PromptInput.js` | 150 |
| `cpp_core/src/proxy_configure_ports_build.cpp` | 150 |
| `src/layouts/BrewlateLayout.js` | 148 |
| `src/api/orchestrateApi.js` | 148 |

## Planned Splits

### 1. `CodeDisplay.js` 153 → ~85 LOC
Extract hook `useCodeDisplay.js` (~60 LOC):
- State: `isEditable`, `editedCode`, `copyFeedback`, refs
- Effects: sync `editedCode` from incoming, reset on stream, auto-scroll
- Handlers: `handleCopy`, `handleSave`, `handleFileOpen`, `toggleAutoScroll`

### 2. `AppHeader.js` 150 → ~75 LOC
Extract `AppHeaderAppearance.js` (~65 LOC):
- `showAppearance` state, dropdown JSX with layout + theme button lists
- Props: `theme`, `layout`, `onSetTheme`, `onSetLayout`

### 3. `PromptInput.js` 150 → ~90 LOC
Extract `PromptInputActions.js` (~50 LOC):
- Backend toggle buttons (llama/mlx)
- Submit button and Quality Pass button
- Props: `backend`, `onBackendChange`, `loading`, `disabled`, `prompt`, `canContinue`, `onQualityPass`, `temperature`, `submitLabel`, `submitLoadingLabel`, `qualityPassLabel`

### 4. `proxy_configure_ports_build.cpp` 150 → ~80 LOC
Extract `proxy_configure_ports_assign.h` inline helper (~65 LOC) as `ports_assign` namespace:
- `pick_port(fixed_port, next_port, fixed_ports)` lambda extracted as free function
- `assign_key(bk, model, sg, fixed_port)` → port-key string builder
- `merge_group(PortGroup& g, agent, bk, model)` → group merge + conflict check

### 5. `BrewlateLayout.js` 148 → ~90 LOC
Extract `useBrewlateLayout.js` hook (~55 LOC):
- `deployed`/`rightTab`/`showMonitor`/`showAgentsPopout`/`leftPopout` state
- `useDeploy` call and `handleDeployedInternal` callback
- `online`/`activeAgents` deploy-sync effect
- Returns: `{ deployed, setDeployed, rightTab, setRightTab, showMonitor, setShowMonitor, showAgentsPopout, setShowAgentsPopout, leftPopout, setLeftPopout, status, statusMsg, agentStatuses, deploy, reset }`

### 6. `orchestrateApi.js` 148 → ~65 LOC
Extract `orchestrateApi.stream.js` (~75 LOC):
- `submitOrchestrateStream` (SSE fetch + event-loop reader)
- `splitIntoChunks` helper
- `orchestrateApi.js` keeps `saveOrchestrateHistory` and `submitOrchestrate`; re-exports stream exports for backwards compat

## Test Plan
- `npm test -- --watchAll=false` → 924 tests pass
- `pytest tests/test_modes.py tests/test_streaming.py -q` → 28 tests pass
