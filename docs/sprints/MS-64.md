# MS-64 Sprint — Modular Split

**Branch:** `cursor/ms-64-sprint`
**Ceiling:** 110 LOC per file

## Files Over Ceiling

| File | LOC |
|---|---|
| `cpp_core/src/config/coordinator_config_validate_entries.h` | 128 |
| `src/App.js` | 125 |
| `src/editor/grid/GridEditor.js` | 122 |
| `cpp_core/src/proxy_routes_convert_jobs.cpp` | 117 |
| `cpp_core/src/coordinator_setup.cpp` | 116 |
| `cpp_core/src/modes/pipeline.cpp` | 116 |

## Planned Splits

### 1. `coordinator_config_validate_entries.h` 128 → ~90 LOC
Extract `coordinator_config_validate_presets.h` as `coordinator_config::impl` namespace inline (~40 LOC):
- `validate_preset_entry(preset_name, p, r)` — validates preset object: mode string, agents array entries, synthesizer string, max_select integer >= 1
- `coordinator_config_validate_entries.h` keeps `warn`, `mode_registered`, `validate_string_option`, `validate_mode_entry`

### 2. `App.js` 125 → ~90 LOC
Extract `useAppCallbacks.js` (~35 LOC):
- `useAppCallbacks({ showToast })` — returns `onModeWarning`, `onMemoryPressureWarning`, `onSaveCodeToast` as stable `useCallback` refs
- `App.js` keeps hook wiring, layout dispatch, `layoutProps` assembly

### 3. `GridEditor.js` 122 → ~80 LOC
Extract `GridEditorPanels.js` (~45 LOC):
- `PanelChip({ name, isDragging })` — draggable panel chip using `useDraggable`
- `SlotZone({ slotName, panelName, onClear })` — droppable slot using `useDroppable`
- `GridEditor.js` keeps `DndContext`, template picker, grid area, `DragOverlay`

### 4. `proxy_routes_convert_jobs.cpp` 117 → ~75 LOC
Extract `proxy_routes_convert_jobs_io.h` as `convert_io` namespace inline (~45 LOC):
- `save_jobs(jobs, mu)` — serialises `convert_jobs` map to `/tmp/matrix-convert-jobs.json` atomically
- `load_jobs(jobs, mu)` — reads and deserialises the jobs JSON file on startup
- `proxy_routes_convert_jobs.cpp` keeps `convert_make_job_id`, `convert_refresh_job`, `convert_job_to_json`

### 5. `coordinator_setup.cpp` 116 → ~75 LOC
Extract `coordinator_setup_wire.h` as `setup_wire` namespace inline (~45 LOC):
- `wire_agents(state, config)` — populates `state.agents` from config JSON, calls `init_port_concurrency`
- `apply_coordinator_section(state, config)` — loads modes_config, presets, default_mode from coordinator block
- `coordinator_setup.cpp` keeps `coordinator_load_config`, `coordinator_set_state_paths`

### 6. `pipeline.cpp` 116 → ~80 LOC
Extract `pipeline_stage_run.h` as `pipeline_stage_run` namespace inline (~40 LOC):
- `run_stage_loop(ctx, by_name, effective_order, stage_context_chars, agent_outputs, stage_outputs, executed, missing, errors, prev_agent, prev_output, final_output, stage_compaction)` — the per-stage iteration (steps 49–81)
- `pipeline.cpp` keeps meta setup, order resolution, fallback/synthesizer dispatch, result assembly

## Test Plan
- `npm test -- --watchAll=false` → 924 tests pass
- `pytest tests/test_modes.py tests/test_streaming.py -q` → 28 tests pass
