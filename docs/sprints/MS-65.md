# MS-65 Sprint — Modular Split

**Branch:** `cursor/ms-65-sprint`
**Ceiling:** 110 LOC per file

## Files Over Ceiling

| File | LOC |
|---|---|
| `src/App.js` | 122 |
| `cpp_core/src/coordinator_routes_architect_stream_pipeline.cpp` | 116 |
| `src/components/RagSources.js` | 115 |
| `cpp_core/src/synthesis_budget.cpp` | 115 |
| `src/editor/freeform/FreeformEditor.js` | 114 |
| `scripts/hero_mlx.py` | 114 |

## Planned Splits

### 1. `App.js` 122 → ~80 LOC
Extract `useAppCoreState.js` (~45 LOC):
- Calls `useSwarm`, `useCoordinatorState`, `useMemo(warningsByMode)`, `useMemoryPressure`, `useAppState` in sequence
- Returns all combined fields: responses, agentErrors, finalAnswer, loading, error, history, online, submit, loadHistory, checkStatus, setResponses, setFinalAnswer, lastMeta, setLastMeta, currentSession, setCurrentSession, backend, switchBackend, activeAgents, modes, activeMode, kvReadings, kvFetchFailed, hostMemory, flatPickAgent, setFlatPickAgent, modeWarnings, refreshModes, refreshAgents, handleModeChange, showConfig, setShowConfig, deployPending, setDeployPending, showHelp, setShowHelp, showConverter, setShowConverter, showRagAdmin, setShowRagAdmin, showCachePanel, setShowCachePanel, cacheStatus, setCacheStatus, useRag, setUseRag, warningsByModeWithMemory, memoryPressure
- `App.js` keeps layout/polling/session/submit/action hooks, layoutProps assembly, Layout render

### 2. `coordinator_routes_architect_stream_pipeline.cpp` 116 → ~65 LOC
Extract `coordinator_routes_architect_stream_router.cpp` (~55 LOC):
- `run_stream_router_mode(agents, cfg, prompt, cancel, write_event, outputs)` — classifier selection, routing logic, `stream_parallel_agents` dispatch
- Declare `run_stream_router_mode` in `coordinator_routes_architect_stream_modes.h`
- `pipeline.cpp` keeps only `run_stream_pipeline_mode`

### 3. `RagSources.js` 115 → ~55 LOC
Extract `RagHitRow.js` (~60 LOC):
- `basename(p)`, `formatDistance(d)` pure helpers
- `HitRow({ h, i })` component with expand/collapse state
- `RagSources.js` keeps `RagSources` component (header, table shell, hit mapping)

### 4. `synthesis_budget.cpp` 115 → ~75 LOC
Extract `synthesis_budget_stream.h` as `synthesis_budget` namespace inline (~40 LOC):
- `build_stream_synthesis_prompt(user_prompt, contributors_in_order, synthesizer)` — stream variant with "contributed" framing
- `synthesis_budget.cpp` keeps `effective_max_prompt_chars`, `build_pipeline_synthesis_prompt`, `build_cascade_synthesis_prompt`

### 5. `FreeformEditor.js` 114 → ~45 LOC
Extract `FreeformPanel.js` (~70 LOC):
- `FreeformPanel({ panel, canvasRef, dispatch })` with drag/resize mouse handlers
- `FreeformEditor.js` keeps palette list, canvas, panel mapping, empty-state hint

### 6. `hero_mlx.py` 114 → ~70 LOC
Extract `hero_mlx_session.py` (~45 LOC):
- `run_session(page, base_dir, frame_secs)` — browser session body: launch, backend switch, profile select, online wait, scenario loop, console error report; returns `all_videos`
- `hero_mlx.py` keeps `main()` as thin orchestrator: makedirs, preflight checks, playwright launch, `run_session` call, `open_videos`

## Test Plan
- `npm test -- --watchAll=false` → 924 tests pass
- `pytest tests/test_modes.py tests/test_streaming.py -q` → 28 tests pass
