# MS-37 — Coordinator Misc/Agents Meta + ModelConverter + Synthesis Budget

**Sprint:** November 1 – November 14, 2026  
**Status:** Done  
**Goal:** Split four ~220–230 LOC production files into focused modules under the 250 LOC limit.

## Context

After MS-34–36, the largest coordinator and UI files on `main` were:

1. **`coordinator_routes_misc.cpp` (230)** — KV clear/evict, response cache routes, memory/metrics/history, CORS
2. **`coordinator_routes_agents_meta.cpp` (225)** — PUT prompt/description/tokens
3. **`ModelConverter.js` (224)** — HF repo guessing, per-row conversion UI, standalone shell
4. **`synthesis_budget.cpp` (223)** — `assemble_fit` truncation + pipeline/cascade/stream prompt builders

## Backlog

| ID | Title | Effort | Status |
|----|-------|--------|--------|
| MS-37-1 | Extract KV ops + cache routes from misc | S | Done |
| MS-37-2 | Extract agent tokens route from agents_meta | S | Done |
| MS-37-3 | Split ModelConverter guess vs row | S | Done |
| MS-37-4 | Extract synthesis assemble/truncate | S | Done |

## Item notes

### MS-37-1 — coordinator misc

- `coordinator_kv_ops.cpp/.h` — parallel KV clear + slot evict on agent ports
- `coordinator_routes_cache.cpp/.h` — `/api/cache*` routes + startup cache config
- `coordinator_routes_misc.cpp` — memory, pressure, metrics, history/entry, CORS (~98 LOC)

### MS-37-2 — agents meta

- `coordinator_routes_agent_tokens.cpp/.h` — PUT `/api/agents/:name/tokens`
- `coordinator_routes_agents_meta.cpp` — prompt + description (~113 LOC)

### MS-37-3 — ModelConverter

- `modelConverterGuess.js` — `ORG_PREFIXES`, `guessHfRepo`, `guessOutputName`
- `ModelConverterRow.js` — per-model conversion form + polling
- `ModelConverter.js` — standalone/embedded shell (~61 LOC)

### MS-37-4 — synthesis budget

- `synthesis_budget_assemble.cpp/.h` — `truncate_note`, `assemble_fit`
- `synthesis_budget.cpp` — `effective_max_prompt_chars` + three public builders (~115 LOC)

## Build script

`scripts/build_cpp_binaries.sh` links:

- `synthesis_budget_assemble.cpp`
- `coordinator_routes_agent_tokens.cpp`
- `coordinator_kv_ops.cpp`
- `coordinator_routes_cache.cpp`

## Verification

```bash
bash scripts/build_cpp_binaries.sh
python3 -m pytest tests/test_modes.py tests/test_streaming.py -q
npm test -- --watchAll=false

wc -l cpp_core/src/coordinator_routes_misc.cpp cpp_core/src/coordinator_kv_ops.cpp \
       cpp_core/src/coordinator_routes_cache.cpp \
       cpp_core/src/coordinator_routes_agents_meta.cpp cpp_core/src/coordinator_routes_agent_tokens.cpp \
       cpp_core/src/synthesis_budget.cpp cpp_core/src/synthesis_budget_assemble.cpp \
       src/components/ModelConverter.js src/components/modelConverterGuess.js \
       src/components/ModelConverterRow.js
```
