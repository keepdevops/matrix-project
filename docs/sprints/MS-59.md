# MS-59 Sprint — Modular Split

**Branch:** `cursor/ms-59-sprint`
**Ceiling:** 110 LOC per file

## Files Over Ceiling

| File | LOC |
|---|---|
| `cpp_core/src/coordinator_routes_modes_put.cpp` | 135 |
| `scripts/hero_mlx.py` | 134 |
| `cpp_core/src/config_service_main.cpp` | 132 |
| `scripts/demo_playwright_actions.py` | 131 |
| `src/layouts/DashboardLayout.js` | 130 |
| `src/layouts/BrewAgentCard.js` | 128 |

## Planned Splits

### 1. `coordinator_routes_modes_put.cpp` 135 → ~80 LOC
Extract `coordinator_routes_modes_put_apply.h` as `modes_put_apply` namespace inline (~55 LOC):
- `apply_string_option(body, config, key, present)` — nullable string setter extracted from lambda
- `apply_order(body, config, mode_name, active_names)` — pipeline order normalizer
- `apply_synthesizer(body, config, active_names, unknown_synth_out)` — synthesizer field handler
- `coordinator_routes_modes_put.cpp` calls `modes_put_apply::` helpers instead of inline lambdas

### 2. `hero_mlx.py` 134 → ~75 LOC
Extract `hero_mlx_checks.py` (~55 LOC):
- `check_dev_server(url)` — urlopen probe + sys.exit on failure
- `check_mlx_coordinator(url)` — urlopen probe + sys.exit on failure
- `open_videos(videos)` — subprocess open + summary print
- `hero_mlx.py` keeps constants, `main` orchestration; delegates preflight + teardown to helpers

### 3. `config_service_main.cpp` 132 → ~75 LOC
Extract `config_service_handlers.h` as `config_svc` namespace inline (~55 LOC):
- `read_doc(path)` + `atomic_write_doc(path, doc)` — file I/O helpers
- `validate_put_body(doc)` — structural validation for PUT
- `config_service_main.cpp` keeps `main` + server setup + route registration

### 4. `demo_playwright_actions.py` 131 → ~75 LOC
Extract `demo_playwright_wait.py` (~55 LOC):
- `wait_for_response(page, shots_dir, shot_idx, label)` — completion polling loop
- `follow_up(page, text, shots_dir, shot_idx, label)` — reply-box submit + wait
- `demo_playwright_actions.py` keeps `shot`, `log`, `ensure_config_*`, `select_profile`, `launch_and_wait_online`, `set_mode`, `broadcast`; re-exports from `demo_playwright_wait`

### 5. `DashboardLayout.js` 130 → ~75 LOC
Extract `DashboardLayoutOverlays.js` (~50 LOC):
- Stats bar (DashboardStatTile × 6, PressureCluster)
- Banners (excludedBreaker, error)
- Overlay panels (ModelConverter, SwarmConfig, history dropdown, HelpModal, RagAdmin, CachePanel)
- `DashboardLayout.js` keeps header + computed stats + `DashboardLayoutGrid`

### 6. `BrewAgentCard.js` 128 → ~75 LOC
Extract `BrewAgentCardBody.js` (~50 LOC):
- Model select / model display block (lines 80–100)
- Footer row: meta + Edit + Expand buttons
- Children slot
- `BrewAgentCard.js` keeps hero image, name row, checkbox, card class, click/keyboard handlers

## Test Plan
- `npm test -- --watchAll=false` → 924 tests pass
- `pytest tests/test_modes.py tests/test_streaming.py -q` → 28 tests pass
