# MS-58 Sprint — Modular Split

**Branch:** `cursor/ms-58-sprint`
**Ceiling:** 110 LOC per file

## Files Over Ceiling

| File | LOC |
|---|---|
| `cpp_core/src/modes/router_dispatch.cpp` | 139 |
| `src/components/ModelConverterRow.js` | 138 |
| `scripts/demo_playwright.py` | 137 |
| `cpp_core/src/modes/pipeline.cpp` | 135 |
| `cpp_core/src/proxy_models_scan.cpp` | 133 |
| `src/components/SwarmAgentSelector.js` | 131 |

## Planned Splits

### 1. `router_dispatch.cpp` 139 → ~85 LOC
Extract `router_dispatch_collect.h` as `router_dispatch_collect` namespace inline (~50 LOC):
- `collect_agent_outputs(st, meta)` — futures gather loop
- `launch_agents(st, by_name, ctx)` — shared agent-launch loop extracted from `apply_directives_and_dispatch` and `apply_fallback_selection`
- `apply_fallback_selection` moves to header; `apply_directives_and_dispatch` + `make_on_chunk` stay in cpp

### 2. `ModelConverterRow.js` 138 → ~80 LOC
Extract `useModelConverterRow.js` (~50 LOC):
- State: `open`, `hfRepo`, `outputName`, `qBits`, `hfToken`, `job`, `error`, `pollRef`
- `stopPolling`, `start` async handler (startConversion + pollConversion loop)
- Derived: `busy`, `done`

### 3. `demo_playwright.py` 137 → ~70 LOC
Extract `demo_playwright_rounds.py` (~65 LOC):
- `run_round_a(page, shots_dir)` — BALANCED profile → ROUTER mode → 2 prompts + follow-up
- `run_round_b(page, shots_dir)` — FLAT mode → 1 prompt
- `run_round_c(page, shots_dir)` — SAFE profile re-launch → CASCADE mode → 1 prompt + follow-up
- `demo_playwright.py` keeps config constants + `run_demo` orchestration + `main`

### 4. `pipeline.cpp` 135 → ~80 LOC
Extract `pipeline_stage_loop.h` as `pipeline_stage` namespace inline (~55 LOC):
- `compact_prev_output(prev_output, prev_agent, stage_context_chars, step, stage_compaction)` — context-window compaction helper
- `run_stage(name, ctx, by_name, prev_agent, prev_output, preset, step, total, agent_outputs, stage_outputs, stage_compaction, executed, errors)` — single-stage execute + record
- `pipeline.cpp` calls `pipeline_stage::run_stage` for each step in the loop

### 5. `proxy_models_scan.cpp` 133 → ~75 LOC
Extract `proxy_models_scan_impl.h` as `models_scan_impl` namespace inline (~55 LOC):
- `is_gguf_name(n)` — extension check
- `gguf_size(path)` + `is_real_gguf(path)` — size/validity guards
- `mlx_dir_size(dir)` — safetensors size aggregator
- `proxy_models_scan.cpp` keeps `scan_dir` + public `scan_models` + `scan_mlx_models`

### 6. `SwarmAgentSelector.js` 131 → ~80 LOC
Extract `SwarmAgentSelectorEngine.js` (~45 LOC):
- Engine pills row (ENGINES map, count badges, disabled logic)
- Profile preset buttons (PROFILE_SAFE/BALANCED/MAX/MIXED)
- Props: `models`, `engine`, `hasEngineModels`, `activeProfile`, `onEngineChange`, `onApplyProfile`

## Test Plan
- `npm test -- --watchAll=false` → 924 tests pass
- `pytest tests/test_modes.py tests/test_streaming.py -q` → 28 tests pass
