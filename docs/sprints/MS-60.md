# MS-60 Sprint — Modular Split

**Branch:** `cursor/ms-60-sprint`
**Ceiling:** 110 LOC per file

## Files Over Ceiling

| File | LOC |
|---|---|
| `src/components/RagIngestPanel.js` | 128 |
| `cpp_core/src/coordinator_routes_dispatch.cpp` | 128 |
| `cpp_core/src/coordinator_routes_architect_stream.cpp` | 128 |
| `src/layouts/SidebarLayout.js` | 127 |
| `src/layouts/BrewEditRoleModal.js` | 127 |
| `cpp_core/src/proxy_configure_health.cpp` | 127 |

## Planned Splits

### 1. `RagIngestPanel.js` 128 → ~75 LOC
Extract `useRagIngestPanel.js` (~55 LOC):
- State: `uploads`, `busy`, `fileRef`, `mountedRef`
- Polling `useEffect` (pending jobs → `ragIngestJob` interval loop)
- `onFiles(files)` — upload loop with per-file status tracking
- `RagIngestPanel.js` keeps render JSX only (drop zone, file input, table)

### 2. `coordinator_routes_dispatch.cpp` 128 → ~80 LOC
Extract `coordinator_routes_dispatch_meta.h` as `dispatch_meta` namespace inline (~50 LOC):
- `build_envelope_meta(envelope, dreq, rag, excluded_unhealthy, qp_target, dispatch_t0)` — stamps session_id, run_id, followup, rag, quality_pass, parent_run_id, compaction, timings, wall_ms onto envelope
- `coordinator_routes_dispatch.cpp` calls `dispatch_meta::build_envelope_meta` instead of inline stamping

### 3. `coordinator_routes_architect_stream.cpp` 128 → ~80 LOC
Extract `coordinator_routes_architect_stream_parse.h` as `stream_parse` namespace inline (~50 LOC):
- `parse_stream_request(body, user_prompt, session_id, parent_run_id, temperature, followup, context_policy)` — JSON parse + field extraction
- `coordinator_routes_architect_stream.cpp` calls `stream_parse::parse_stream_request` instead of inline parsing block

### 4. `SidebarLayout.js` 127 → ~75 LOC
Extract `SidebarLayoutPanels.js` (~55 LOC):
- History dropdown, overlay panels (ModelConverter, SwarmConfig, HelpModal, RagAdmin, CachePanel)
- Props: all show* flags + onClose callbacks + history/onHistorySelect + onDeployed
- `SidebarLayout.js` keeps AppHeader + PromptInput + AgentGrid + MetricsStrip grid

### 5. `BrewEditRoleModal.js` 127 → ~75 LOC
Extract `useBrewEditRoleModal.js` (~55 LOC):
- State: `tab`, `saving`, `error`, `prompt`, `tokens`
- `handleSave` async handler (setAgentSystemPrompt + setAgentTokens + onSaved)
- `BrewEditRoleModal.js` keeps render: tab bar + BrewBasicTab / BrewAdvancedTab + footer buttons

### 6. `proxy_configure_health.cpp` 127 → ~75 LOC
Extract `proxy_configure_health_checks.h` as `health_checks` namespace inline (~55 LOC):
- Per-agent health probe helper (HTTP GET + timeout + parse response)
- Aggregate health summary builder
- `proxy_configure_health.cpp` keeps route registration + public `configure_health_routes`

## Test Plan
- `npm test -- --watchAll=false` → 924 tests pass
- `pytest tests/test_modes.py tests/test_streaming.py -q` → 28 tests pass
