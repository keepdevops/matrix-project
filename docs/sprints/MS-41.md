# MS-41 — Proxy Configure Ports + Prompt/Swarm/Brew UI Splits

**Sprint:** May 18 – May 31, 2026  
**Status:** Done  
**Goal:** Split `proxy_configure_ports.cpp` and three ~200 LOC React configure/prompt files under the 250 LOC limit.

## Context

After MS-37–40, remaining production files near the limit:

1. **`proxy_configure_ports.cpp` (~230)** — port binding, `PortGroup` assembly, active-config write
2. **`PromptInput.js` (~201)** — RAG/mode state, effects, submit handlers mixed with form UI
3. **`SwarmConfig.js` (~200)** — load/effects, profile handlers, derived layout/risk + deploy shell
4. **`BrewConfigPanel.js` (~198)** — engines/profile chrome + large agents card grid

## Backlog

| ID | Title | Effort | Status |
|----|-------|--------|--------|
| MS-41-1 | Split proxy configure ports (build + write) | S | Done |
| MS-41-2 | Extract `usePromptInput` hook | S | Done |
| MS-41-3 | Extract `useSwarmConfigState` hook | S | Done |
| MS-41-4 | Extract `BrewConfigAgentsSection` | S | Done |

## Item notes

### MS-41-1 — proxy configure ports

- `proxy_configure_ports_build.cpp` — `proxy_configure_build_port_groups`, port availability, `PortGroup` merge
- `proxy_configure_ports_write.cpp` — `proxy_configure_write_active_config` (active swarm JSON)
- `proxy_configure_ports.h` — `PortBuildResult`, declarations; `PortGroup` / `PROXY_CONFIGURE_DOCKER_PORT` from `proxy_configure_internal.h`
- `proxy_configure.cpp` — calls build then write on configure path

### MS-41-2 — PromptInput

- `usePromptInput.js` — RAG persistence, external prompt/temperature sync, textarea auto-grow, submit/key handlers
- `PromptInput.js` — form shell importing hook (~150 LOC)

### MS-41-3 — SwarmConfig

- `useSwarmConfigState.js` — fetch effect, engine/profile handlers, `computeLayout` + vLLM ports, risk/backends
- `SwarmConfig.js` — offline retry UI, `SwarmAgentSelector` + `ServerLayoutPreview` shell (~95 LOC)

### MS-41-4 — BrewConfigPanel

- `BrewConfigAgentsSection.js` — agents header, budgets popout, `BrewAgentCard` grid + result expand
- `BrewConfigPanel.js` — header, monitor, engines, profile, footer launch (~145 LOC)

## Build script

`scripts/build_cpp_binaries.sh` links:

- `proxy_configure_ports_build.cpp`
- `proxy_configure_ports_write.cpp`

(replaces `proxy_configure_ports.cpp`)

## Verification

```bash
bash scripts/build_cpp_binaries.sh
python3 -m pytest tests/ui/test_buttons.py -q
python3 -m pytest tests/test_modes.py tests/test_streaming.py -q
npm test -- --watchAll=false

wc -l cpp_core/src/proxy_configure_ports_build.cpp cpp_core/src/proxy_configure_ports_write.cpp \
       cpp_core/src/proxy_configure_ports.h \
       src/components/PromptInput.js src/components/usePromptInput.js \
       src/components/SwarmConfig.js src/components/useSwarmConfigState.js \
       src/layouts/BrewConfigPanel.js src/layouts/BrewConfigAgentsSection.js
```
