# MS-40 — Proxy Routes + Coordinator Startup + Pipeline Run + Code Save ZIP

**Sprint:** December 13 – December 26, 2026  
**Status:** Done  
**Goal:** Split four ~201–204 LOC production files before they cross 250 LOC.

## Context

MS-39 (#146) covers dashboard/sidebar, pressure evict, session store. MS-36 (#137) still owns the top proxy/coordinator UI files. Next tier on `main`:

1. **`proxy_routes.cpp` (204)** — local API routes + coordinator forward catch-all
2. **`modes/pipeline.cpp` (202)** — pipeline stage loop + synthesis
3. **`coordinator.cpp` (202)** — config load + agent roster + HTTP server
4. **`codeSave.js` (201)** — code file collection + store-only ZIP export

## Backlog

| ID | Title | Effort | Status |
|----|-------|--------|--------|
| MS-40-1 | Split proxy local routes vs coordinator forward | S | Done |
| MS-40-2 | Extract pipeline run handler | S | Done |
| MS-40-3 | Extract coordinator startup/load | S | Done |
| MS-40-4 | Extract codeSave ZIP helpers | S | Done |

## Item notes

### MS-40-1 — proxy_routes

- `proxy_routes_local.cpp/.h` — models, configure, logs, status, clear-cache, forward
- `proxy_routes.cpp` — CORS, orchestrate/convert registration shell (~20 LOC)

### MS-40-2 — pipeline mode

- `pipeline_run.cpp/.h` — `run_pipeline_mode` stage loop + synthesis
- `pipeline.cpp` — mode registration only (~16 LOC)

### MS-40-3 — coordinator

- `coordinator_startup.cpp/.h` — config load, validation, agents, history/sessions
- `coordinator.cpp` — telemetry, routes, listen (~55 LOC)

### MS-40-4 — codeSave

- `codeSaveZip.js` — CRC32, UTF-8, store-only ZIP blob
- `codeSave.js` — collect/export/download (re-exports `buildZipBlob`)

## Build script

- `coordinator_startup.cpp` (coordinator target)
- `proxy_routes_local.cpp` (proxy target)
- `pipeline_run.cpp` (auto via `modes/*.cpp` glob)

## Verification

```bash
bash scripts/build_cpp_binaries.sh
npm test -- --testPathPattern=codeSave --watchAll=false
```

## Open PRs

- **#146** MS-39, **#145** MS-38, **#137** MS-36, **#143**, **#144**
