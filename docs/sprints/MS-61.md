# MS-61 Sprint — Modular Split

**Branch:** `cursor/ms-61-sprint`
**Ceiling:** 110 LOC per file

## Files Over Ceiling

| File | LOC |
|---|---|
| `cpp_core/src/coordinator_routes_dispatch_prepare.cpp` | 127 |
| `src/api/streamApi.js` | 125 |
| `scripts/hero_expand.py` | 125 |
| `src/layouts/BrewlateLayout.js` | 124 |
| `src/components/HelpModalReference.js` | 124 |
| `cpp_core/src/coordinator_routes_modes.cpp` | 124 |

## Planned Splits

### 1. `coordinator_routes_dispatch_prepare.cpp` 127 → ~75 LOC
Extract `coordinator_routes_dispatch_rag.h` as `dispatch_rag` namespace inline (~55 LOC):
- `build_rag_block(dreq, agents)` — retrieves RAG context and formats the rag block string
- `resolve_rag_agents(body, active_agents)` — parses `rag_agents` field from request body
- `coordinator_routes_dispatch_prepare.cpp` keeps `dispatch_parse_request` + `dispatch_build_rag` wiring

### 2. `streamApi.js` 125 → ~75 LOC
Extract `streamApi.submit.js` (~55 LOC):
- `submitPromptStream(prompt, temperature, opts, callbacks)` — full SSE streaming function
- `streamApi.js` keeps `submitPromptNonStream`, `submitOrchestrate`, re-exports from submit module

### 3. `hero_expand.py` 125 → ~75 LOC
Extract `hero_expand_scenarios.py` (~55 LOC):
- `run_router_expand(page, shots_dir, frame_secs)` — ROUTER mode → broadcast → expand 3 cards
- `run_flat_expand(page, shots_dir, frame_secs)` — re-deploy → FLAT mode → broadcast → expand 3 cards
- `hero_expand.py` keeps constants, preflight checks, `main` orchestration

### 4. `BrewlateLayout.js` 124 → ~85 LOC
Extract `BrewlateLayoutGrid.js` (~40 LOC):
- Main content grid: PromptInput, ConversationThread, AgentGrid, FinalAnswerPanel, RagSources, PipelineStageOutputs, CompareVariantsPanel
- `BrewlateLayout.js` keeps AppHeader, sidebar panels, useDeploy call, setDeployed(true) visibility

### 5. `HelpModalReference.js` 124 → ~75 LOC
Extract `HelpModalReferenceTable.js` (~50 LOC):
- The keyboard shortcuts / API reference table rows component
- `HelpModalReference.js` keeps section headers and orchestrates table rendering

### 6. `coordinator_routes_modes.cpp` 124 → ~75 LOC
Extract `coordinator_routes_modes_config.h` as `modes_config` namespace inline (~50 LOC):
- `read_mode_config(st, mode_name)` — locked read of `st.modes_config[mode_name]`
- `build_mode_agents_response(st, mode_name, cfg)` — assembles the GET /api/modes/:name/agents JSON
- `coordinator_routes_modes.cpp` keeps route registration; delegates config reads to helpers

## Test Plan
- `npm test -- --watchAll=false` → 924 tests pass
- `pytest tests/test_modes.py tests/test_streaming.py -q` → 28 tests pass
