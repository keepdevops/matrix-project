# MS-36 — Proxy Convert Jobs + Dispatch RAG + HelpModal + BrewEditRole

**Sprint:** November 15 – November 28, 2026  
**Status:** Done  
**Goal:** Split the next four production files in the 228–238 LOC band after MS-31–34 landed on `main`.

## Context

With MS-31–34 merged, remaining near-limit files:

1. **`proxy_routes_convert.cpp` (238)** — job store + route handlers interleaved
2. **`coordinator_routes_dispatch.cpp` (234)** — RAG prep block dominates the handler
3. **`HelpModal.js` (232)** — static copy + agent role list in one file
4. **`BrewEditRoleModal.js` (228)** — shared toggle/slider widgets inline

## Backlog

| ID | Title | Effort | Status |
|----|-------|--------|--------|
| MS-36-1 | Extract `proxy_convert_jobs` from convert routes | S | Done |
| MS-36-2 | Extract `coordinator_dispatch_rag` from architect dispatch | S | Done |
| MS-36-3 | Split HelpModal static sections + agent descriptions | S | Done |
| MS-36-4 | Extract `brewEditRoleControls` from BrewEditRoleModal | S | Done |

## PR stack

| PR | Scope |
|----|-------|
| *(to be filled as PR lands)* | |

## Verification

```bash
bash scripts/build_cpp_binaries.sh
python3 -m pytest tests/test_modes.py tests/test_streaming.py -q
npm test -- --watchAll=false

wc -l cpp_core/src/proxy_routes_convert.cpp cpp_core/src/proxy_convert_jobs.cpp \
       cpp_core/src/coordinator_routes_dispatch.cpp cpp_core/src/coordinator_dispatch_rag.cpp \
       src/components/HelpModal.js src/components/HelpModalSections.js \
       src/layouts/BrewEditRoleModal.js src/layouts/brewEditRoleControls.js
```
