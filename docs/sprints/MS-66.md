# MS-66 Sprint — Modular Split

**Branch:** `cursor/ms-66-sprint`
**Ceiling:** 110 LOC per file

## Files Over Ceiling

| File | LOC |
|---|---|
| `src/components/usePromptInput.js` | 113 |
| `cpp_core/src/coordinator_routes_agents_meta.cpp` | 113 |
| `cpp_core/src/code_fence_normalize.cpp` | 113 |
| `cpp_core/src/agent_health.cpp` | 112 |
| `src/editor/EditorContext.js` | 111 |
| `src/components/TokenBudgetGrid.js` | 111 |

## Planned Splits

### 1. `usePromptInput.js` 113 → ~75 LOC
Extract `usePromptInputRag.js` (~40 LOC):
- RAG state: `ragTopK`, `ragMinScore`, `selectedRagAgents` with localStorage-initialised defaults
- `useEffect` debounce that persists `rag.top_k` and `rag.min_score` to localStorage
- Returns `{ ragTopK, setRagTopK, ragMinScore, setRagMinScore, selectedRagAgents, setSelectedRagAgents }`
- `usePromptInput.js` calls `usePromptInputRag()`, keeps submit logic, textarea effects, external prompt/temp effects

### 2. `coordinator_routes_agents_meta.cpp` 113 → ~65 LOC
Extract `coordinator_routes_agents_desc.h` as inline (~50 LOC):
- `register_agent_description_route(svr, st)` inline — the `/api/agents/:name/description` PUT handler: validates body, checks roster, persists via `persist_agent_description`, updates live agents, logs result
- `coordinator_routes_agents_meta.cpp` keeps the `/api/agents/:name/prompt` PUT handler and the `register_coordinator_routes_agent_tokens` call

### 3. `code_fence_normalize.cpp` 113 → ~65 LOC
Extract `code_fence_parse.h` as `code_fence::impl` namespace inline (~50 LOC):
- `FenceBlock` struct
- `trim_copy(s)`, `parse_lang_token(info_line)`, `extract_fences(raw)` inline helpers
- `code_fence_normalize.cpp` keeps `code_agents()`, `reserved_entry_keys()`, `is_code_history_agent`, `normalize_for_history`, `normalize_agents_in_entry`

### 4. `agent_health.cpp` 112 → ~80 LOC
Extract `agent_health_snapshot.h` as `agent_health` namespace inline (~35 LOC):
- `snapshot()` inline — builds JSON object with per-agent failure/tripped/cooldown state plus `__config` block
- `agent_health.cpp` keeps `Entry` struct, statics (`g_state`, `g_mu`), `now_ms`, `prune_window`, `record`, `is_open`

### 5. `EditorContext.js` 111 → ~40 LOC
Extract `editorReducer.js` (~75 LOC):
- `DEFAULT_FLOW_NODES` array
- `initialState` object
- `reducer(state, action)` function with all 13 action cases
- `EditorContext.js` keeps `EditorProvider`, `useEditor`, context creation, imports

### 6. `TokenBudgetGrid.js` 111 → ~50 LOC
Extract `TokenBudgetRow.js` (~65 LOC):
- `TokenBudgetRow({ role, drafts, errors, notices, busy, isDirty, setDraft, saveOne })` component — per-role inputs (ctx, max_tokens, timeout, gpu, concurrency) + Save button
- Import constants from `TokenBudgetGrid.js`
- `TokenBudgetGrid.js` keeps exported constants, grid header row, `visibleRoles.map` delegation

## Test Plan
- `npm test -- --watchAll=false` → 924 tests pass
- `pytest tests/test_modes.py tests/test_streaming.py -q` → 28 tests pass
