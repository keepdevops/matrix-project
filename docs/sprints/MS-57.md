# MS-57 Sprint — Modular Split

**Branch:** `cursor/ms-57-sprint`
**Ceiling:** 110 LOC per file

## Files Over Ceiling

| File | LOC |
|---|---|
| `src/components/ModeParamControls.js` | 143 |
| `cpp_core/src/rag_client.cpp` | 143 |
| `src/api/configApi.js` | 142 |
| `cpp_core/src/modes/router_plan_parse.cpp` | 142 |
| `cpp_core/src/mode_module.cpp` | 141 |
| `src/hooks/useSubmitHandlers.js` | 140 |

## Planned Splits

### 1. `ModeParamControls.js` 143 → ~80 LOC
Extract `ModeParamTotControls.js` (~55 LOC):
- Tree-of-Thought sliders: `totDepth`, `totBranching`, `totPruneBelow`
- Props: `totDepth`, `setTotDepth`, `totBranching`, `setTotBranching`, `totPruneBelow`, `setTotPruneBelow`, `loading`, `disabled`
- `ModeParamControls.js` renders `<ModeParamTotControls>` for `isTot` branch

### 2. `configApi.js` 142 → ~75 LOC
Extract `configApi.health.js` (~65 LOC):
- `fetchHostMemory`, `fetchKvPressure`, `clearKvCache`
- `checkHealth`, `setCacheConfig`, `checkRagHealth`
- `configApi.js` keeps `fetchModels`, `fetchSwarmConfig`, vllmApi re-export; re-exports from `configApi.health`

### 3. `useSubmitHandlers.js` 140 → ~85 LOC
Extract `useSubmitHandlers.helpers.js` (~50 LOC):
- `buildQualityPassInstruction()` — instruction string builder
- `buildSendBestContinueOpts(flatPickAgent)` — context policy builder
- `buildSaveCodeResult(activeAgents, responses)` — delegates to `buildCodeExport` + `downloadBlob`
- `useSubmitHandlers.js` calls these pure helpers instead of inlining the logic

### 4. `rag_client.cpp` 143 → ~75 LOC
Extract `rag_client_conn.h` as `rag_conn` namespace inline (~65 LOC):
- `Conn` struct + `conn_singleton()`
- `ensure_open_locked(c, dsn)` — reconnect logic
- `exec_query(c, sql, params)` — PQ query + error handling helper
- `rag_client.cpp` calls `rag_conn::` helpers for DB access

### 5. `router_plan_parse.cpp` 142 → ~80 LOC
Extract `router_plan_parse_impl.h` as `plan_parse_impl` namespace inline (~55 LOC):
- `parse_selected_line(raw, choice_set)` — regex SELECTED: line extractor
- `parse_json_agents(raw, choice_set)` — JSON array fallback parser
- `router_plan_parse.cpp` keeps `as_string_vec`, `parse_router_plan`, `parse_agent_list`; delegates line/JSON parsing to `plan_parse_impl`

### 6. `mode_module.cpp` 141 → ~80 LOC
Extract `mode_module_load.h` as `mode_load` namespace inline (~55 LOC):
- `load_modes_from_file(path)` — file read + JSON parse + validation
- `validate_mode_entry(entry)` — per-mode field checks
- `mode_module.cpp` keeps route handlers and `reload_modes`; delegates load/validate to `mode_load`

## Test Plan
- `npm test -- --watchAll=false` → 924 tests pass
- `pytest tests/test_modes.py tests/test_streaming.py -q` → 28 tests pass
