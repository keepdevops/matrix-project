# MS-62 Sprint — Modular Split

**Branch:** `cursor/ms-62-sprint`
**Ceiling:** 110 LOC per file

## Files Over Ceiling

| File | LOC |
|---|---|
| `cpp_core/src/swarm_config_roster.cpp` | 123 |
| `src/layouts/useTerminalInput.js` | 122 |
| `cpp_core/src/proxy_configure_spawn.cpp` | 122 |
| `scripts/hero_mlx_runner.py` | 121 |
| `src/components/SwarmConfig.helpers.js` | 120 |
| `src/components/KvPressureGauge.js` | 119 |

## Planned Splits

### 1. `swarm_config_roster.cpp` 123 → ~75 LOC
Extract `swarm_config_roster_cache.h` as `roster_cache` namespace inline (~50 LOC):
- `snapshot_names(path)` — reads the config file and returns set of agent names (filesystem + JSON parse)
- `is_stale(snap, path)` — compares mtime + size to detect changes
- `swarm_config_roster.cpp` keeps `swarm_config_get_agent_names` public function + cache update logic

### 2. `useTerminalInput.js` 122 → ~75 LOC
Extract `useTerminalCommands.js` (~50 LOC):
- `buildCommandHandlers({ onModeChange, onSetTheme, onSetLayout, onUseRagChange, onToggleConfig, onOpenCachePanel, onFollowUp, onClearSession, onSubmit })` — returns command dispatch map for `:mode`, `:theme`, `:layout`, `:rag`, `:config`, `:cache`, `:clear`, and prompt submission
- `useTerminalInput.js` keeps state, history navigation, effect hooks, command dispatch call

### 3. `proxy_configure_spawn.cpp` 122 → ~75 LOC
Extract `proxy_configure_spawn_launch.h` as `spawn_launch` namespace inline (~50 LOC):
- `build_launch_args(pg, port, model_path, env)` — assembles argv vector for a given backend (llama/mlx/vllm/docker)
- `proxy_configure_spawn.cpp` keeps the spawn loop, PID tracking, thread startup

### 4. `hero_mlx_runner.py` 121 → ~75 LOC
Extract `hero_mlx_record.py` (~50 LOC):
- `record_scenario_mov(shots_dir, frame_secs, scenario_name)` — ffmpeg ProRes assembly from PNGs
- `expand_agent_cards(page, shots_dir, shot_counter)` — iterates agent cards, clicks ⤢, screenshots
- `hero_mlx_runner.py` keeps `switch_to_mlx_backend`, `run_scenario` orchestration

### 5. `SwarmConfig.helpers.js` 120 → ~75 LOC
Extract `SwarmConfig.portHelpers.js` (~50 LOC):
- `portLabel(port, pg)` — port/backend display string
- `portStatusClass(status)` — CSS class from port status
- `buildPortDisplay(portGroups, portStatuses)` — assembles display list for the ports UI
- `SwarmConfig.helpers.js` keeps `shortName`, `isAppleSilicon`, `ENGINES`, `PROFILE_*` constants, `deriveRosterStatus`

### 6. `KvPressureGauge.js` 119 → ~75 LOC
Extract `useKvPressureGauge.js` (~45 LOC):
- State: `pct`, `color`, `tweenRef`, `rafRef`
- `useEffect` for tween animation loop (TWEEN_MS settle + requestAnimationFrame)
- Derived: `arcPath`, `strokeDash` computations
- `KvPressureGauge.js` keeps SVG render only

## Test Plan
- `npm test -- --watchAll=false` → 924 tests pass
- `pytest tests/test_modes.py tests/test_streaming.py -q` → 28 tests pass
