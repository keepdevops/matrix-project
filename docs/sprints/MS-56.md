# MS-56 Sprint — Modular Split

**Branch:** `cursor/ms-56-sprint`
**Ceiling:** 110 LOC per file

## Files Over Ceiling

| File | LOC |
|---|---|
| `src/App.js` | 148 |
| `src/components/SwarmConfig.deploy.js` | 146 |
| `src/layouts/BrewConfigPanel.js` | 145 |
| `cpp_core/src/session_store.cpp` | 145 |
| `cpp_core/src/proxy_validate_gguf.cpp` | 145 |
| `src/components/AgentPromptModal.js` | 144 |

## Planned Splits

### 1. `App.js` 148 → ~80 LOC
Extract `useAppState.js` (~60 LOC):
- All `useState` declarations: `showConfig`, `deployPending`, `showHelp`, `showConverter`, `showRagAdmin`, `showCachePanel`, `cacheStatus`, `useRag`
- `showConfigPanel` derived value
- `excludedBreaker` + `stageOutputs` derived values
- `warningsByModeWithMemory` memo
- `prevError` effect + toast
- Returns all state values + setters

### 2. `SwarmConfig.deploy.js` 146 → ~75 LOC
Extract `SwarmConfig.deploy.launch.js` (~65 LOC):
- `buildPortAgentMap(layout)` helper
- `startPolling(portAgentMap, pollRef, setAgentStatuses, stopPolling)` helper
- The `deploy` async function body (validation guards + agent-build + configureSwarm call + error handler)
- `useDeploy` keeps state, `stopPolling`, `reset`, and delegates to `launch.js` helpers

### 3. `BrewConfigPanel.js` 145 → ~80 LOC
Extract `BrewConfigEngineProfile.js` (~55 LOC):
- Engine pills section (ENGINES map + pill buttons)
- Profile section (preset select + hint text)
- Props: `models`, `engine`, `handleEngineChange`, `activeProfile`, `applyProfile`, `reset`

### 4. `session_store.cpp` 145 → ~80 LOC
Extract `session_store_context.h` inline helpers as `session_ctx` namespace (~60 LOC):
- `build_context_block(run, include_set, target_agent)` — per-run context assembler
- `trim_block(text, max_chars)` — truncation helper
- `session_build_context` delegates to these helpers

### 5. `proxy_validate_gguf.cpp` 145 → ~80 LOC
Extract `proxy_validate_gguf_impl.h` as `gguf_impl` namespace (~60 LOC):
- `format_hint(buf, n)` — magic-byte format identifier
- `read_u32_le(buf, offset)` + `read_u64_le(buf, offset)` — little-endian readers
- `check_gguf_header(path)` — file-open + magic + version checks
- `proxy_validate_gguf.cpp` keeps only the public `validate_gguf_magic` / `validate_gguf_metadata` shell functions

### 6. `AgentPromptModal.js` 144 → ~80 LOC
Extract `useAgentPromptModal.js` (~55 LOC):
- State: `text`, `desc`, `busy`, `error`
- `useEffect` to sync agent change
- `save` async handler (setAgentDescription + setAgentSystemPrompt calls)
- `handleReset` + derived flags: `promptDirty`, `descDirty`, `dirty`, `canResetDefault`

## Test Plan
- `npm test -- --watchAll=false` → 924 tests pass
- `pytest tests/test_modes.py tests/test_streaming.py -q` → 28 tests pass
