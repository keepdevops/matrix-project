# MS-67 Sprint — Final Modular Split

**Branch:** `cursor/ms-67-sprint`
**Ceiling:** 110 LOC per file

## Files Over Ceiling

| File | LOC |
|---|---|
| `cpp_core/src/proxy_routes.cpp` | 111 |
| `cpp_core/src/coordinator_routes_presets.cpp` | 111 |
| `cpp_core/src/agent_stream.cpp` | 111 |

## Planned Splits

### 1. `proxy_routes.cpp` 111 → ~65 LOC
Extract `proxy_routes_logs.h` as `proxy_logs` namespace inline (~50 LOC):
- `register_logs_route(svr, cors, proj_root)` inline — the `/api/logs` GET handler: parses `ports` query param, reads per-port log files, returns JSON array
- `proxy_routes.cpp` keeps CORS lambda, OPTIONS, `/api/models`, `/api/swarm-config`, `/api/configure`, `/api/configure/status`, `/api/memory`, and delegate calls

### 2. `coordinator_routes_presets.cpp` 111 → ~65 LOC
Extract `coordinator_routes_presets_url.h` as `presets_url` namespace inline (~50 LOC):
- `url_decode(s)` inline — percent-decode + `+`→space
- `register_get_delete_routes(svr, st)` inline — the GET `/api/presets` and DELETE `/api/presets/:name` handlers
- `coordinator_routes_presets.cpp` keeps PUT and POST `/apply` handlers

### 3. `agent_stream.cpp` 111 → ~25 LOC
Extract `agent_stream_llama.h` as `agent_stream` namespace inline (~90 LOC):
- `stream_llama(agent, system_prompt_in, prompt_in, on_chunk, cancel)` inline — builds messages JSON, SSE streaming loop, metrics recording
- `stream_mlx_oneshot(agent, system_prompt, prompt, on_chunk)` inline
- `agent_stream.cpp` keeps only the public `stream_agent` dispatch function

## Test Plan
- `npm test -- --watchAll=false` → 924 tests pass
- `pytest tests/test_modes.py tests/test_streaming.py -q` → 28 tests pass
- **This is the final sprint — all files will be at or under 110 LOC ceiling after merge.**
