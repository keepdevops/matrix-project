# MS-39 — Dashboard/Sidebar Layouts + Pressure Evict + Session Store

**Sprint:** November 29 – December 12, 2026  
**Status:** Done  
**Goal:** Split four ~204–214 LOC production files before they cross 250 LOC.

## Context

MS-38 (#145, open) targets config validate, risk compute, mode roster, rag client. MS-36 (#137) still owns the top four on `main`. Next tier:

1. **`DashboardLayout.js` (214)** — stats bar + two-column grid
2. **`pressure_evict.cpp` (209)** — port state read + slot eviction + HTTP route
3. **`SidebarLayout.js` (206)** — collapsible sidebar + main column
4. **`session_store.cpp` (204)** — session I/O + continuation prompt builder

## Backlog

| ID | Title | Effort | Status |
|----|-------|--------|--------|
| MS-39-1 | Split DashboardLayout stats + main grid | S | Done |
| MS-39-2 | Extract pressure evict port worker | S | Done |
| MS-39-3 | Extract SidebarLayout aside panel | S | Done |
| MS-39-4 | Extract session continuation builder | S | Done |

## Item notes

### MS-39-1 — DashboardLayout

- `DashboardStatTile.js`, `DashboardStatsBar.js` — stat tiles + KV pressure strip
- `DashboardMainGrid.js` — prompt / conversation / agents columns
- `DashboardLayout.js` — header, banners, overlays (~110 LOC)

### MS-39-2 — pressure_evict

- `pressure_evict_port.cpp/.h` — `read_port_state`, `evict_port`
- `pressure_evict.cpp` — `/api/pressure/evict` route registration (~95 LOC)

### MS-39-3 — SidebarLayout

- `SidebarLayoutAside.js` — collapse control, config, history, metrics
- `SidebarLayout.js` — header + main column shell (~130 LOC)

### MS-39-4 — session_store

- `session_store_continuation.cpp/.h` — compaction helpers + `session_build_continuation_impl`
- `session_store.cpp` — load/save/new_id/append_run (~75 LOC)

## Build script

Added `session_store_continuation.cpp`, `pressure_evict_port.cpp`.

## Verification

```bash
bash scripts/build_cpp_binaries.sh
npm test -- --watchAll=false
```

## Open PRs

- **#145** MS-38, **#137** MS-36, **#143** llama ports, **#144** Buttons fix
