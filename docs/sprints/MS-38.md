# MS-38 — Config Validate + Risk Compute + Mode Roster + RAG Client

**Sprint:** November 15 – November 28, 2026  
**Status:** Done  
**Goal:** Split four ~215–223 LOC production files before they cross the 250 LOC limit.

## Context

MS-37 cleared coordinator misc/agents_meta/synthesis and ModelConverter. MS-36 (#137) still covers the top four files on `main` (proxy convert, dispatch RAG, HelpModal, BrewEditRole). Next tier:

1. **`coordinator_config_validate.cpp` (223)** — mode/preset/swarm document validation
2. **`SwarmConfig.risk.js` (222)** — RAM/KV estimate math + RiskCard UI
3. **`ModeRosterPanel.js` (218)** — per-mode roster state + panel chrome
4. **`rag_client.cpp` (215)** — pgvector connection/search + embed sidecar HTTP

## Backlog

| ID | Title | Effort | Status |
|----|-------|--------|--------|
| MS-38-1 | Split config validate modes vs presets | S | Done |
| MS-38-2 | Extract SwarmConfig risk compute vs RiskCard | S | Done |
| MS-38-3 | Extract useModeRosterPanel hook | S | Done |
| MS-38-4 | Split rag_client embed vs pg search | S | Done |

## Item notes

### MS-38-1 — coordinator_config_validate

- `coordinator_config_validate_common.cpp/.h` — warn, mode_registered, string options
- `coordinator_config_validate_modes.cpp` — mode entry + modes object
- `coordinator_config_validate_presets.cpp` — preset entry + presets object
- `coordinator_config_validate.cpp` — swarm document + coordinator block (~72 LOC)

### MS-38-2 — SwarmConfig.risk

- `SwarmConfig.riskCompute.js` — constants, bands, `computeRiskEstimate`
- `SwarmConfigRiskCard.js` — `RiskCard` component
- `SwarmConfig.risk.js` — re-exports (unchanged import path)

### MS-38-3 — ModeRosterPanel

- `useModeRosterPanel.js` — fetch/save state and roster handlers
- `ModeRosterPanel.js` — section UI (~130 LOC)

### MS-38-4 — rag_client

- `rag_client_embed.cpp/.h` — HTTP embed sidecar
- `rag_client_pg.cpp/.h` — libpq connection + vector search
- `rag_client.cpp` — retrieve orchestration, context block, health (~95 LOC)

## Build script

Added to `scripts/build_cpp_binaries.sh` (coordinator + `rag_embed_test`):

- `coordinator_config_validate_{common,modes,presets}.cpp`
- `rag_client_{embed,pg}.cpp`

## Verification

```bash
bash scripts/build_cpp_binaries.sh
python3 -m pytest tests/test_modes.py tests/test_streaming.py -q
npm test -- --watchAll=false
npm test -- --testPathPattern='SwarmConfig.risk|ModeRosterPanel|RosterGrid' --watchAll=false
```

## Open PRs (parallel)

- **#137** MS-36 (draft) — proxy convert, dispatch RAG, HelpModal, BrewEditRole
- **#143** llama port grouping
- **#144** Brewlate Buttons test fix (draft)
