FROM node:20-alpine

WORKDIR /app

# Install dependencies (npm ci for deterministic, lockfile-based installs)
COPY package.json package-lock.json* ./
RUN npm ci --silent

# Copy source code
COPY . .

EXPOSE 3000

CMD ["npm", "start"]
