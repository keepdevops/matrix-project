# Development: hot-reload React (npm start). Mount source via docker-compose.
FROM node:24-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci --silent

COPY . .

EXPOSE 3000

CMD ["npm", "start"]
